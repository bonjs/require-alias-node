{
  "name": "require-alias-node",
  "version": "1.0.2",
  "description": "A tool which could set an alias for the module's path which is required in the require of nodejs",
  "main": "index.js",
  "scripts": {
    "t": "node test/test.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["require", "alias"],
  "author": "Alex bonjs",
  "license": "ISC",
  "dependencies": {
    "caller-dir": "^1.0.2",
    "rimraf": "^2.6.2",
    "stringify-parse": "^1.0.4"
  }
}
